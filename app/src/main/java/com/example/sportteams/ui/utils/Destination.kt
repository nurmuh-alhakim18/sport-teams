package com.example.sportteams.ui.utils

import com.example.sportteams.R

enum class Destination(
    val title: Int,
) {
    CATEGORY_LIST(title = R.string.app_name),
    TEAM_LIST(title = R.string.team_list),
}
